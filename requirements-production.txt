# Production requirements for Seventh Day Sabbath Church Of Christ (ShalomGH)
# Optimized for Fly.io deployment with minimal dependencies for cost efficiency

# Core Django
Django>=5.0,<6.0
django-environ>=0.11.0
whitenoise>=6.6.0
Pillow>=10.0.0

# Forms and UI
django-crispy-forms>=2.0
crispy-tailwind>=0.5.0
django-widget-tweaks>=1.4.12

# Database
dj-database-url>=3.0.0
psycopg2-binary>=2.9.0

# Configuration
python-decouple>=3.8

# Production server
gunicorn>=21.2.0

# Security (lightweight)
django-cors-headers>=4.3.0

# Performance optimization (minimal)
django-compressor>=4.4

# Monitoring (essential only)
sentry-sdk[django]>=1.38.0

# File handling
django-cleanup>=8.0.0
imagekitio>=3.0.0
